# Third-Party Notices

`vendor/mikuproject` is the Apache-2.0 licensed upstream project vendored as a
Git subtree. Its license is retained at `vendor/mikuproject/LICENSE`.

The only Maven dependency declared by this repository is JUnit Jupiter. It has
test scope and is not bundled into the runtime jar or distribution zip.
